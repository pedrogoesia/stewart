Flask>=3.0
python-pptx>=1.0
Pillow>=10.0
# Banco de dados (SQLite no PC, PostgreSQL em produção) e login de usuários
Flask-SQLAlchemy>=3.1
Flask-Login>=0.6
# Segurança: proteção CSRF e limite de requisições (anti força-bruta/abuso)
Flask-WTF>=1.2
Flask-Limiter>=3.5
# Servidor de produção (usado na hospedagem, ex.: Render)
gunicorn>=21.2
# Serve os arquivos estáticos (CSS/JS/imagens) sem ocupar o Flask sob carga
whitenoise>=6.6
# Driver do PostgreSQL — usado só quando DATABASE_URL aponta para um Postgres.
# No SQLite local ele nem é carregado. Tem instalador pronto p/ Windows/Mac/Linux.
psycopg[binary]>=3.1
# Edição de fotos por IA (OpenAI - gpt-image-1) e leitura do arquivo .env
openai>=1.97
python-dotenv>=1.0
# Opcional: suporte a fotos HEIC/HEIF de iPhone
pillow-heif>=0.16
